<apex:page controller="OppertunityList2Con">
  <apex:pageBlock >
      <apex:pageBlockTable value="{!Opportunities}" var="o">
          <apex:column value="{!o.Name}"/>
          <apex:column value="{!o.CloseDate}"/>
      </apex:pageBlockTable>
  </apex:pageBlock>
</apex:page>